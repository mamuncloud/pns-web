interface:
  display_name: "Pns Web"
  short_description: "Repo-specific patterns and workflows for pns-web"
  default_prompt: "Use the pns-web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true